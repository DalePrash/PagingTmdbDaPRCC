package com.example.pagingtmdbdapr.wast;

import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;

import java.util.List;

//@Dao
public interface MovieDao {
/*

  //  @Query("SELECT * FROM PojoBMovieParts ORDER BY movieId ASC")
    //LiveData<List<PojoJ>> getAlphabetizedWords();



   // @Query("SELECT * FROM movieparts_table ORDER BY movieId ASC")
    List<PojoBMovieParts> getAlphabetizedWords();

    // @Insert(onConflict = OnConflictStrategy.IGNORE)
    @Insert
    void insert(PojoBMovieParts nTitle);

    @Delete
    void delete(PojoBMovieParts nTitle);

    @Query("DELETE FROM movieparts_table")
    void deleteAll();

    // @RawQuery("SELECT * FROM news_table")

    @Query("SELECT * FROM movieparts_table")
    List<PojoBMovieParts> getGenders();

*/
}
