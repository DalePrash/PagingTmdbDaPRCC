package com.example.pagingtmdbdapr.ui;

import android.os.Bundle;

import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;
import androidx.databinding.DataBindingUtil;
import androidx.lifecycle.ViewModelProviders;

import com.example.pagingtmdbdapr.R;
import com.example.pagingtmdbdapr.customview.CustomViewViewModel;

public class ConstraintActivityPractise extends AppCompatActivity {

    CustomViewViewModel customViewViewModel;
 //   ActivityConstraintModelBinding binding;
    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
     //   setContentView(R.layout.activity_constraint_practise);
   //    setContentView(R.layout.activity_constraint_model);
    //    DataBindingUtil.setContentView(this, R.layout.activity_main);
    //    ActivityMainBinding binding = DataBindingUtil.setContentView(this, R.layout.activity_main);
      //  customViewViewModel = ViewModelProviders.of(this).get(CustomViewViewModel.class);
     //  customViewViewModel.setLanguage("English");

   //     binding = DataBindingUtil.setContentView(this, R.layout.activity_constraint_model);
        //ActivityBlurBinding binding = DataBindingUtil.setContentView(this, R.layout.activity_blur);


    }
}
